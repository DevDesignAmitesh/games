services:
  # postgres:
  #   image: postgres:alpine
  #   ports:
  #     - 5432:5432
  #   environment:
  #     - POSTGRES_DB=games
  #     - POSTGRES_USER=postgres
  #     - POSTGRES_PASSWORD=postgres

  # redis:
  #   image: redis:alpine
  #   ports:
  #     - 6379:6379

  http:
    image: amitesh252/games-http-be:latest
    ports:
      - 3001:4000
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET=${JWT_SECRET}
      - REDIS_URL=${REDIS_URL}
  ws:
    image: amitesh252/games-ws-be:latest
    ports:
      - 3002:8080
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET=${JWT_SECRET}
      - REDIS_URL=${REDIS_URL}

  # client:
  #   image: amitesh252/games-fe:latest
  #   ports:
  #     - 3000:3000
  #   environment:
  #     - NEXT_PUBLIC_NODE_ENV=${NEXT_PUBLIC_NODE_ENV}

